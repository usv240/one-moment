{
  "name": "@one-moment/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Holds the call together: two AssemblyAI realtime streams on the caller, a Voice Agent session on the far party whose LLM is our own Adjudicator endpoint.",
  "scripts": {
    "start": "node --env-file-if-exists=../../.env src/server.ts"
  },
  "dependencies": {
    "@one-moment/core": "*",
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "cloudflared": "^0.7.1"
  }
}
